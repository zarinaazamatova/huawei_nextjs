import React from 'react';
import { Layout } from '@/components/Layout';
import { News } from '@/components/News';

const Home = () => {
  return (
    <Layout>
      <News />
    </Layout>
  );
};

export default Home;
